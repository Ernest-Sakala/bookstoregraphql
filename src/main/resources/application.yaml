spring:
  application:
    name: bookstoregraphql

  datasource:
    url: jdbc:postgresql://localhost:5432/bookstore
    username: postgres
    password: ernest13579!
    driver-class-name: org.postgresql.Driver

  servlet:
    multipart:
      max-file-size: 100MB
      max-request-size: 100MB

  graphql:
    graphiql:
      enabled: true
      path: /graphiql

# CHANGE in production: openssl rand -base64 64
jwt:
  secret: bG9uZ1JhbmRvbVNlY3JldEtleUZvckpXVFNpZ25pbmdUaGF0SXNBZ29vZExlbmd0aA==
  expiration-ms: 86400000

  # ── CORS ────────────────────────────────────────────────────────────
cors:
  allowed-origins: http://localhost:4200

  # ── Logging ─────────────────────────────────────────────────────────
logging:
  level:
    com:
      bookstore: DEBUG
    org:
      springframework:
        graphql: INFO
